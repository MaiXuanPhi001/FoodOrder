import { View, Text } from 'react-native'
import React from 'react'
import Box from '~/atoms/Box'
import { colors } from '~/themes/colors'

const Header = () => {
  return (
    <Box h={50} bg={colors.background} w={'100%'}>
        
    </Box>
  )
}

export default Header